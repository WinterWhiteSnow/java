mysql 
실행과정?은 먼저 스키마 파일의 테이블 구조부터 올리고
그다음에 data에 있는 테이블기준으로 만들어진 데이터들을 commit,
이후에 다른 파일에서 작업들을 실행함
스키마 = 어떤 테이블이 어떤 구성으로 돼 있는지 골격을 잡는 것
데이터 = 그 테이블의 스키마를 기준으로 데이터를 삽입 및 삭제, 변경 등을 하는 것

 

create table or schema,
mysql는 table이나 schema나 특별히 구분하지 않는다고 함!
그래서 대충
create table `user` (
해당 테이블의 컬럼들을 작성,
작성할때마다 콤마를 사용해서 구분해주고,
맨 마지막 컬럼은 뒤에 아무것도 안붙임
보통 구조는,
컬럼명 데이터타입 기타기능(primary key, not null, auto머시기(알아서 크기 올라가는것, 
만약 10까지 도달한 시점에서 0을 집어넣으면 0이 반영되는 것이 아닌 11로 반영이됨))
)뒤에 뭔가 덧붙였는데.. 어떤 기능인지 잘 모르겠음;

alter 데이터 변경.. 인데 삭제일때도 사용함, 뭔가 다용도로 사용하는듯?
insert into <- 데이터 삽입
desc <- 이게 정렬할때도 사용햇었지만, 줄임말로 써서 똑같아 보이지 사실 descript의 줄임말이라
select같은 기능임! 보통 그 테이블의 스키마(구조)를 볼 때 사용하는듯?
desc user 이걸 하면
스키마로 넣었을때 id,pw,date등등의 요소들이 나옴 뼈대이기때문에 데이터자체가 나오는건 아님!
데이터를 보고 싶으면
select *
from user;
이걸 사용함